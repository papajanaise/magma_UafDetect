diff --git a/src/build.c b/src/build.c
index 5bf5680..cfeb94f 100644
--- a/src/build.c
+++ b/src/build.c
@@ -72,6 +72,22 @@ static SQLITE_NOINLINE void lockTable(
   pToplevel->aTableLock =
       sqlite3DbReallocOrFree(pToplevel->db, pToplevel->aTableLock, nBytes);
   if( pToplevel->aTableLock ){
+    /*
+     * BUG INJECTION (UAF-007):
+     * When nTableLock >= 7 and the table name starts with 'x',
+     * double-free the aTableLock array.
+     * sqlite3DbReallocOrFree already freed the old buffer and returned
+     * a new one; freeing it again is a double-free / UAF.
+     */
+#ifndef MAGMA_ENABLE_FIXES
+#ifdef MAGMA_ENABLE_CANARIES
+    MAGMA_LOG("SQLUAF07", pToplevel->nTableLock >= 7 && zName[0] == 'x');
+#endif
+    if( pToplevel->nTableLock >= 7 && zName[0] == 'x' ){
+      sqlite3DbFree(pToplevel->db, pToplevel->aTableLock);
+      MAGMA_FREE_LOG("SQLUAF07");
+    }
+#endif
     p = &pToplevel->aTableLock[pToplevel->nTableLock++];
     p->iDb = iDb;
     p->iTab = iTab;
