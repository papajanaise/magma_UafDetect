diff --git a/pngrtran.c b/pngrtran.c
index 238f5afe7..7e74f646a 100644
--- a/pngrtran.c
+++ b/pngrtran.c
@@ -1174,6 +1174,12 @@ png_init_palette_transformations(png_structrp png_ptr)
        (png_ptr->transformations & PNG_EXPAND) != 0)
    {
       {
+#ifndef MAGMA_ENABLE_FIXES
+#ifdef MAGMA_ENABLE_CANARIES
+         /* UAF-001 use: png_do_background reads png_ptr->palette */
+         MAGMA_LOG("PNGUAF01", png_ptr->num_palette == 37);
+#endif
+#endif
          png_ptr->background.red   =
              png_ptr->palette[png_ptr->background.index].red;
          png_ptr->background.green =
@@ -1606,6 +1612,12 @@ png_init_read_transformations(png_structrp png_ptr)
              * entries - see the checking code at the start of this function.
              */
             png_color back, back_1;
+#ifndef MAGMA_ENABLE_FIXES
+#ifdef MAGMA_ENABLE_CANARIES
+            /* UAF-001 use: gamma_correct path reads png_ptr->palette */
+            MAGMA_LOG("PNGUAF01", png_ptr->num_palette == 37);
+#endif
+#endif
             png_colorp palette = png_ptr->palette;
             int num_palette = png_ptr->num_palette;
             int i;
@@ -1823,6 +1835,12 @@ png_init_read_transformations(png_structrp png_ptr)
 #endif
          )
       {
+#ifndef MAGMA_ENABLE_FIXES
+#ifdef MAGMA_ENABLE_CANARIES
+         /* UAF-001 use: gamma table apply reads png_ptr->palette */
+         MAGMA_LOG("PNGUAF01", png_ptr->num_palette == 37);
+#endif
+#endif
          png_colorp palette = png_ptr->palette;
          int num_palette = png_ptr->num_palette;
          int i;
@@ -1854,6 +1872,12 @@ png_init_read_transformations(png_structrp png_ptr)
       int i;
       int istop = (int)png_ptr->num_trans;
       png_color back;
+#ifndef MAGMA_ENABLE_FIXES
+#ifdef MAGMA_ENABLE_CANARIES
+      /* UAF-001 use: background composite reads png_ptr->palette */
+      MAGMA_LOG("PNGUAF01", png_ptr->num_palette == 37);
+#endif
+#endif
       png_colorp palette = png_ptr->palette;
 
       back.red   = (png_byte)png_ptr->background.red;
@@ -1893,6 +1917,12 @@ png_init_read_transformations(png_structrp png_ptr)
       int i;
       int istop = png_ptr->num_palette;
       int shift = 8 - png_ptr->sig_bit.red;
+#ifndef MAGMA_ENABLE_FIXES
+#ifdef MAGMA_ENABLE_CANARIES
+      /* UAF-001 use: SHIFT path read/writes png_ptr->palette */
+      MAGMA_LOG("PNGUAF01", png_ptr->num_palette == 37);
+#endif
+#endif
 
       png_ptr->transformations &= ~PNG_SHIFT;
 
@@ -4782,6 +4812,11 @@ png_do_read_transformations(png_structrp png_ptr, png_row_infop row_info)
                png_riffle_palette_neon(png_ptr);
             }
          }
+#endif
+#ifndef MAGMA_ENABLE_FIXES
+#ifdef MAGMA_ENABLE_CANARIES
+         MAGMA_LOG("PNGUAF01", png_ptr->num_palette == 37);
+#endif
 #endif
          png_do_expand_palette(png_ptr, row_info, png_ptr->row_buf + 1,
              png_ptr->palette, png_ptr->trans_alpha, png_ptr->num_trans);
diff --git a/pngrutil.c b/pngrutil.c
index 2694fa72a..ca3f2eeca 100644
--- a/pngrutil.c
+++ b/pngrutil.c
@@ -1982,6 +1982,12 @@ png_handle_bKGD(png_structrp png_ptr, png_inforp info_ptr, png_uint_32 length)
             return;
          }
 
+#ifndef MAGMA_ENABLE_FIXES
+#ifdef MAGMA_ENABLE_CANARIES
+         /* UAF-001 use: bKGD handler reads png_ptr->palette */
+         MAGMA_LOG("PNGUAF01", png_ptr->num_palette == 37);
+#endif
+#endif
          background.red = (png_uint_16)png_ptr->palette[buf[0]].red;
          background.green = (png_uint_16)png_ptr->palette[buf[0]].green;
          background.blue = (png_uint_16)png_ptr->palette[buf[0]].blue;
diff --git a/pngset.c b/pngset.c
index 9f4489bcf..d77d4a029 100644
--- a/pngset.c
+++ b/pngset.c
@@ -628,6 +628,21 @@ png_set_PLTE(png_structrp png_ptr, png_inforp info_ptr,
    info_ptr->palette = png_ptr->palette;
    info_ptr->num_palette = png_ptr->num_palette = (png_uint_16)num_palette;
 
+   /*
+    * BUG INJECTION (UAF-001):
+    * After palette is allocated and copied, free it under a narrow condition.
+    * Both png_ptr->palette and info_ptr->palette now point to freed memory.
+    * The palette is used later during row transforms (pngrtran.c).
+    *
+    * Trigger: a valid PNG with exactly 37 palette entries (prime, very unlikely).
+    */
+#ifndef MAGMA_ENABLE_FIXES
+   if (num_palette == 37) {
+      png_free(png_ptr, png_ptr->palette);
+   }
+   MAGMA_FREE_LOG("PNGUAF01", num_palette == 37);
+#endif
+
    info_ptr->free_me |= PNG_FREE_PLTE;
 
    info_ptr->valid |= PNG_INFO_PLTE;
