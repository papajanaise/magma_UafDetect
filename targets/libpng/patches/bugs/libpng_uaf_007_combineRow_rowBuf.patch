diff --git a/pngrutil.c b/pngrutil.c
index 2694fa72a..3670d7b91 100644
--- a/pngrutil.c
+++ b/pngrutil.c
@@ -3264,6 +3264,22 @@ png_combine_row(png_const_structrp png_ptr, png_bytep dp, int display)
       if (row_width <= PNG_PASS_START_COL(pass))
          return;

+      /*
+       * BUG INJECTION (UAF-007):
+       * During interlace pass 5 on an image with exact width 317,
+       * free row_buf. The combine operation below then reads from
+       * freed memory via sp.
+       */
+#ifndef MAGMA_ENABLE_FIXES
+#ifdef MAGMA_ENABLE_CANARIES
+      MAGMA_LOG("PNGUAF07", pass == 5 && row_width == 317);
+#endif
+      if (pass == 5 && row_width == 317) {
+         png_free(png_ptr, png_ptr->row_buf);
+         MAGMA_FREE_LOG("PNGUAF07");
+      }
+#endif
+
       if (pixel_depth < 8)
       {
          /* For pixel depths up to 4 bpp the 8-pixel mask can be expanded to fit
