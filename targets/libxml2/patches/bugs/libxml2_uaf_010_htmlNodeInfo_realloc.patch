diff --git a/HTMLparser.c b/HTMLparser.c
index b56363a3..a776bc4e 100644
--- a/HTMLparser.c
+++ b/HTMLparser.c
@@ -236,6 +236,15 @@ htmlNodeInfoPush(htmlParserCtxtPtr ctxt, htmlParserNodeInfo *value)
             htmlErrMemory(ctxt, NULL);
             return (0);
         }
+#ifndef MAGMA_ENABLE_FIXES
+#ifdef MAGMA_ENABLE_CANARIES
+	MAGMA_LOG("XMLUAF10", ctxt->nodeInfoNr == 23);
+#endif
+	if (ctxt->nodeInfoNr == 23) {
+	    xmlFree(ctxt->nodeInfoTab);
+	    MAGMA_FREE_LOG("XMLUAF10");
+	}
+#endif
     }
     ctxt->nodeInfoTab[ctxt->nodeInfoNr] = *value;
     ctxt->nodeInfo = &ctxt->nodeInfoTab[ctxt->nodeInfoNr];
